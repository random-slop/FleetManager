# Архитектура проекта FleetManager

## Общая концепция

**FleetManager** — Qt-приложение для управления парком строительной техники с использованием паттерна **Model-View** и SQLite для хранения данных.

---

## Структура проекта

```
FleetManager/
├── src/
│   ├── models/                    # Модели данных (бизнес-логика)
│   │   ├── Machine.h/cpp          ✅ Класс техники
│   │   └── Project.h/cpp          ✅ Класс проекта
│   │
│   ├── database/                  # Слой работы с БД
│   │   ├── FleetDatabase.h        ✅ Singleton для работы с SQLite
│   │   └── FleetDatabase.cpp      ✅ CRUD операции
│   │
│   ├── ui/                        # Пользовательский интерфейс
│   │   ├── MainWindow.ui          ✅ Макет главного окна (Qt Designer)
│   │   ├── MainWindow.h           ✅ Заголовок главного окна
│   │   ├── MainWindow.cpp         🔄 СОЗДАЁМ СЕЙЧАС
│   │   ├── MachineTableModel.h    ✅ Модель таблицы
│   │   ├── MachineTableModel.cpp  ✅ Реализация модели
│   │   ├── MachineDialog.h        ❌ TODO: Диалог добавления/редактирования
│   │   └── MachineDialog.cpp      ❌ TODO
│   │
│   ├── main.cpp                   🔄 Точка входа (нужно обновить)
│   └── CMakeLists.txt             🔄 Нужно обновить
│
├── Методичка.md                   📄 Методические указания
├── Чеклист.md                     📄 Чек-лист для проверки
└── ARCHITECTURE.md                📄 Этот файл
```

---

## Реализованные компоненты

### ✅ 1. Модели данных

#### [`Machine`](src/models/Machine.h)
- Поля: ID, название, тип, серийный номер, год выпуска, статус, стоимость, текущий проект
- Статусы: `Available`, `OnSite`, `InRepair`, `Decommissioned`
- Методы: геттеры/сеттеры, `statusToString()`, `stringToStatus()`
- **Принцип ООП:** Инкапсуляция (все поля `private`)

#### [`Project`](src/models/Project.h)
- Поля: ID, название, дата начала, дата окончания, описание
- Метод: `isActive()` — проверка активности проекта

### ✅ 2. База данных

#### [`FleetDatabase`](src/database/FleetDatabase.h)
- **Паттерн:** Singleton
- **Таблицы:** `machines`, `projects`
- **Операции:**
  - CRUD для техники: `addMachine()`, `updateMachine()`, `deleteMachine()`, `getAllMachines()`
  - CRUD для проектов: `addProject()`, `updateProject()`, `deleteProject()`, `getAllProjects()`
  - Фильтрация: `getMachinesByStatus()`
  - Статистика: `getStatistics()` — количество техники по статусам
- **Тестовые данные:** Автоматическое создание 12 единиц техники и 3 проектов при первом запуске

### ✅ 3. Модель таблицы

#### [`MachineTableModel`](src/ui/MachineTableModel.h)
- Наследуется от `QAbstractTableModel`
- 7 колонок: Название | Тип | Серийный номер | Год | Статус | Стоимость | Проект
- **Фильтрация:** `setStatusFilter(int)` — показывать только технику с определённым статусом
- **Цветовое кодирование:**
  - Свободна → зелёный
  - На объекте → синий
  - В ремонте → оранжевый
  - Списана → красный

### ✅ 4. Главное окно (частично)

#### [`MainWindow`](src/ui/MainWindow.h)
- **Меню:** Файл, Редактировать, Вид, Техника, Проекты, Отчёты, Справка
- **Toolbar:** Добавить, Редактировать, Удалить, Назначить на проект, Вернуть, В ремонт
- **Структура:**
  - Боковая панель навигации (слева)
  - Центральная таблица с техникой
  - Панель деталей (справа)
  - Статусбар (статистика)

---

## Что нужно доделать (TODO)

### 🔄 1. Завершить `MainWindow.cpp`
**Функции:**
- `setupUI()` — инициализация виджетов
- `setupSidebar()` — создание навигации
- `setupTable()` — настройка QTableView
- `setupDetailsPanel()` — панель с деталями выбранной техники
- `onAddMachine()` — диалог добавления
- `onEditMachine()` — диалог редактирования
- `onDeleteMachine()` — удаление с подтверждением
- `onAssignToProject()` — назначение на проект
- `onReturnFromProject()` — возврат с проекта (статус → Свободна)
- `onSendToRepair()` — отправка в ремонт
- `updateDetailsPanel()` — обновление информации справа
- `updateStatusBar()` — обновление статистики внизу

### ❌ 2. Создать `MachineDialog` (диалог добавления/редактирования)
**Поля:**
- Название (QLineEdit)
- Тип (QComboBox: Экскаватор, Кран, Бульдозер, ...)
- Серийный номер (QLineEdit)
- Год выпуска (QSpinBox)
- Стоимость (QDoubleSpinBox)
**Кнопки:** OK, Отмена
**Валидация:** Все поля обязательны, серийный номер уникален

### 🔄 3. Обновить `CMakeLists.txt`
Добавить все новые файлы в список исходников:
- `ui/MainWindow.cpp`, `ui/MainWindow.ui`
- `ui/MachineTableModel.cpp`
- `ui/MachineDialog.cpp`, `ui/MachineDialog.ui` (когда создадим)

### 🔄 4. Обновить `main.cpp`
```cpp
int main(int argc, char* argv[]) {
    QApplication app(argc, argv);
    
    // Инициализация базы данных
    FleetDatabase::instance().initialize("fleet.db");
    
    // Создание и показ главного окна
    MainWindow window;
    window.show();
    
    return app.exec();
}
```

### ❌ 5. Первая компиляция и тестирование
- Собрать проект через CMake
- Проверить работу таблицы
- Проверить фильтрацию
- Проверить панель деталей

---

## Принципы ООП в проекте

### 1. **Инкапсуляция**
- Все поля классов `private`
- Доступ только через геттеры/сеттеры
- Пример: [`Machine::getName()`](src/models/Machine.h:48)

### 2. **Наследование**
- [`MachineTableModel`](src/ui/MachineTableModel.h) наследует `QAbstractTableModel`
- [`MainWindow`](src/ui/MainWindow.h) наследует `QMainWindow`

### 3. **Полиморфизм**
- Переопределение виртуальных методов `QAbstractTableModel`:
  - `rowCount()`, `columnCount()`, `data()`, `headerData()`

### 4. **Паттерны проектирования**
- **Singleton:** [`FleetDatabase`](src/database/FleetDatabase.h) — один экземпляр на всё приложение
- **Model-View:** Разделение данных ([`MachineTableModel`](src/ui/MachineTableModel.h)) и представления ([`QTableView`](https://doc.qt.io/qt-6/qtableview.html))

---

## Технологии

| Технология | Версия | Назначение |
|------------|--------|------------|
| **C++** | C++20 | Язык программирования |
| **Qt** | Qt6 | GUI-фреймворк |
| **CMake** | 3.21+ | Система сборки |
| **SQLite** | Встроена в Qt | База данных |
| **Visual Studio 2026** | — | IDE/Редактор |

---

## Следующие шаги

1. ✅ Создать `MainWindow.cpp`
2. ✅ Создать `MachineDialog.h/cpp/ui`
3. ✅ Обновить `CMakeLists.txt`
4. ✅ Обновить `main.cpp`
5. 🔧 Скомпилировать и протестировать
6. 📝 Добавить функционал проектов (назначение техники)
7. 🔍 Добавить поиск и расширенную фильтрацию
8. 📊 Добавить отчёты (статистика, история использования)
9. 📄 Написать пояснительную записку

---

## Соответствие методичке

### Требования
- ✅ **ООП:** Классы `Machine`, `Project`, `FleetDatabase`, `MachineTableModel`
- ✅ **Оконное приложение:** Qt-интерфейс
- ✅ **C++:** C++20 с умными указателями (`std::shared_ptr`)
- ✅ **Комментарии:** Doxygen-style комментарии для всех классов и методов
- ✅ **Читаемость:** Отступы, пробелы, понятные имена переменных

### Этапы разработки (по ЕСПД)
1. ✅ **Техническое задание:** Определены функции, входные/выходные данные
2. ✅ **Эскизный проект:** Выбраны методы (SQLite, Model-View), структура данных
3. 🔄 **Технический проект:** Диаграмма классов (TODO), детальные алгоритмы
4. 🔄 **Рабочий проект:** Программирование (в процессе), тестирование
5. ❌ **Внедрение:** Передача программы и документации

---

## Структура базы данных

### Таблица `machines`
| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INTEGER PRIMARY KEY | Уникальный идентификатор |
| `name` | TEXT NOT NULL | Название техники |
| `type` | TEXT NOT NULL | Тип (Экскаватор, Кран, ...) |
| `serial_number` | TEXT UNIQUE NOT NULL | Серийный номер |
| `year_of_manufacture` | INTEGER NOT NULL | Год выпуска |
| `status` | TEXT NOT NULL | Статус (Свободна, На объекте, ...) |
| `cost` | REAL NOT NULL | Стоимость в рублях |
| `current_project` | TEXT | Название текущего проекта |
| `assigned_date` | TEXT | Дата назначения (ISO 8601) |

### Таблица `projects`
| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INTEGER PRIMARY KEY | Уникальный идентификатор |
| `name` | TEXT UNIQUE NOT NULL | Название проекта |
| `start_date` | TEXT NOT NULL | Дата начала (ISO 8601) |
| `end_date` | TEXT | Дата окончания (NULL = активен) |
| `description` | TEXT | Описание проекта |

---

**Версия:** 1.0  
**Дата:** 20.01.2026  
**Автор:** Вячеслав
