# Roadmap разработки FleetManager

## ✅ Milestone 1: Базовая версия (ЗАВЕРШЕНО)

- [x] Архитектура проекта и модели данных
- [x] База данных SQLite с CRUD операциями
- [x] Главное окно с таблицей техники
- [x] Фильтрация по статусам
- [x] Панель деталей
- [x] Базовые операции (удаление, смена статуса)
- [x] Документация (README, ARCHITECTURE)

---

## 🔄 Milestone 2: Полный функционал управления (В РАБОТЕ)

### Приоритет 1: Критичный функционал

#### 1.1 Диалог добавления/редактирования техники
**Зачем:** Сейчас нельзя добавлять новую технику — можно только работать с тестовыми данными.

**Что делать:**
- [ ] Создать [`MachineDialog.ui`](src/ui/MachineDialog.ui) в Qt Designer
- [ ] Реализовать [`MachineDialog.h/cpp`](src/ui/MachineDialog.h)
- [ ] Поля формы:
  - Название (QLineEdit)
  - Тип (QComboBox: Экскаватор, Кран, Бульдозер, Погрузчик, и т.д.)
  - Серийный номер (QLineEdit с проверкой уникальности)
  - Год выпуска (QSpinBox, диапазон 1990-2030)
  - Стоимость (QDoubleSpinBox, формат с ₽)
  - Статус (QComboBox, по умолчанию "Свободна")
- [ ] Валидация:
  - Все поля обязательны
  - Серийный номер уникален (проверять в БД)
  - Год не больше текущего+1
  - Стоимость > 0
- [ ] Подключить в [`MainWindow::onAddMachine()`](src/ui/MainWindow.cpp:285) и `onEditMachine()`

**Технические детали:**
```cpp
class MachineDialog : public QDialog {
    Q_OBJECT
public:
    explicit MachineDialog(QWidget *parent = nullptr, MachinePtr machine = nullptr);
    MachinePtr getMachine() const;
private:
    void setupUI();
    bool validate();
    void fillFromMachine(MachinePtr machine);
};
```

#### 1.2 Поиск по технике
**Зачем:** При большом количестве техники (50+) нужен быстрый поиск.

**Что делать:**
- [ ] Добавить [`QLineEdit`](https://doc.qt.io/qt-6/qlineedit.html) для поиска в toolbar
- [ ] Реализовать фильтрацию в [`MachineTableModel`](src/ui/MachineTableModel.h):
  - По названию (частичное совпадение)
  - По серийному номеру
  - По типу
- [ ] Комбинирование фильтров (статус + поиск)

**Технические детали:**
```cpp
// В MachineTableModel добавить:
void setSearchFilter(const QString& searchText);
private:
    QString m_searchFilter;
    void applyAllFilters(); // Применяет И статус, И поиск
```

---

### Приоритет 2: Важный функционал

#### 2.1 Управление проектами
**Зачем:** Сейчас проекты создаются только как тестовые данные. Нужно управлять ими.

**Что делать:**
- [ ] Создать [`ProjectDialog.ui/h/cpp`](src/ui/ProjectDialog.h) — диалог добавления/редактирования проекта
- [ ] Поля:
  - Название (QLineEdit)
  - Дата начала (QDateEdit)
  - Дата окончания (QDateEdit, опционально)
  - Описание (QTextEdit)
- [ ] Создать [`ProjectsView.ui/h/cpp`](src/ui/ProjectsView.h) — отдельная вкладка с проектами
- [ ] Таблица проектов с колонками:
  - Название
  - Дата начала
  - Дата окончания
  - Количество техники
  - Статус (Активен/Завершён)
- [ ] Подключить кнопку навигации "🏗️ Проекты" в sidebar

#### 2.2 Назначение техники на проект
**Зачем:** Диалог для выбора проекта при назначении техники.

**Что делать:**
- [ ] Создать [`AssignDialog.ui/h/cpp`](src/ui/AssignDialog.h)
- [ ] Поля:
  - QComboBox со списком активных проектов
  - Дата назначения (QDateEdit, по умолчанию сегодня)
  - Примечание (QTextEdit, опционально)
- [ ] При назначении:
  - Статус → "На объекте"
  - current_project → название проекта
  - assigned_date → выбранная дата
- [ ] Подключить в [`MainWindow::onAssignToProject()`](src/ui/MainWindow.cpp:312)

---

### Приоритет 3: Улучшения UX

#### 3.1 История операций
**Зачем:** Отслеживать кто, когда и что делал с техникой.

**Что делать:**
- [ ] Создать таблицу `history` в БД:
  ```sql
  CREATE TABLE history (
      id INTEGER PRIMARY KEY,
      machine_id INTEGER,
      operation TEXT,  -- "added", "edited", "deleted", "assigned", "returned", "repair"
      timestamp TEXT,
      details TEXT,    -- JSON с деталями операции
      FOREIGN KEY (machine_id) REFERENCES machines(id)
  );
  ```
- [ ] Добавить методы в [`FleetDatabase`](src/database/FleetDatabase.h):
  - `addHistoryRecord(machineId, operation, details)`
  - `getHistoryForMachine(machineId)`
  - `getAllHistory()`
- [ ] Создать [`HistoryView.ui/h/cpp`](src/ui/HistoryView.h) — вкладка с историей
- [ ] Подключить кнопку "📜 История" в sidebar

#### 3.2 Статистика и отчёты
**Зачем:** Видеть общую картину парка, экспортировать данные.

**Что делать:**
- [ ] Создать [`StatisticsWidget.h/cpp`](src/ui/StatisticsWidget.h)
- [ ] Виджеты:
  - QLabel с количеством техники по типам
  - QChartView (если подключить Qt Charts) — круговая диаграмма статусов
  - Общая стоимость парка
  - Средний возраст техники
- [ ] Экспорт в CSV:
  - Список всей техники
  - Список техники на проектах
  - Статистика по проектам

---

## 🎨 Milestone 3: Улучшение интерфейса

### 3.1 Темы оформления
- [ ] Тёмная тема (уже есть частично)
- [ ] Светлая тема
- [ ] Переключатель в настройках

### 3.2 Иконки
- [ ] Добавить иконки для типов техники (🚜, 🏗️, 🚚)
- [ ] Иконки для кнопок toolbar
- [ ] Создать [`resources.qrc`](resources.qrc) для Qt Resource System

### 3.3 Настройки
- [ ] Создать [`SettingsDialog.ui/h/cpp`](src/ui/SettingsDialog.h)
- [ ] Настройки:
  - Путь к БД
  - Тема оформления
  - Формат дат
  - Валюта (₽, $, €)
  - Язык интерфейса (RU/EN)

---

## 📄 Milestone 4: Документация и защита

### 4.1 Диаграмма классов UML
**Зачем:** Требуется по методичке.

**Что делать:**
- [ ] Создать диаграмму в PlantUML или Dia
- [ ] Показать все классы:
  - Machine, Project
  - FleetDatabase
  - MainWindow, MachineDialog, MachineTableModel
- [ ] Связи между классами (наследование, композиция, ассоциация)
- [ ] Сохранить в [`docs/class_diagram.png`](docs/class_diagram.png)

### 4.2 Пояснительная записка
**Формат:** 20-25 страниц, Times New Roman 14pt, ГОСТ.

**Разделы:**
1. **Введение** (1-2 стр)
   - Актуальность
   - Цель и задачи курсовой работы
2. **Анализ предметной области** (2-3 стр)
   - Что такое парк техники
   - Существующие аналоги (Excel, 1С, специализированные системы)
   - Обоснование выбора настольного приложения
3. **Техническое задание** (3-4 стр)
   - Назначение разработки
   - Функциональные требования
   - Нефункциональные требования (производительность, надёжность)
   - Выбор технологий (Qt, SQLite, C++)
4. **Моделирование и разработка** (5-7 стр)
   - Постановка задачи
   - Проектирование классов
   - **Диаграмма классов UML**
   - Описание ООП (инкапсуляция, наследование, полиморфизм)
   - Паттерны проектирования (Singleton, Model-View)
5. **Описание программы** (4-5 стр)
   - Алгоритм функционирования
   - Структура базы данных
   - Логическая структура (взаимодействие модулей)
   - Тестирование и отладка
6. **Инструкция по эксплуатации** (2-3 стр)
   - Системные требования
   - Установка и запуск
   - Описание интерфейса
   - Контрольный пример (добавить технику, назначить на проект)
7. **Заключение** (1 стр)
   - Достигнутые результаты
   - Перспективы развития
8. **Список литературы** (1 стр)
   - Документация Qt
   - Стандарты ГОСТ
   - Учебники по C++
9. **Приложения**
   - Листинг основных классов (Machine, FleetDatabase, MainWindow)
   - Скриншоты интерфейса

### 4.3 Презентация для защиты
- [ ] 5-7 слайдов PowerPoint/LibreOffice Impress
- [ ] Содержание:
  1. Титульный слайд (тема, автор)
  2. Постановка задачи и актуальность
  3. Архитектура приложения (диаграмма)
  4. Основной функционал (скриншоты)
  5. Технологии и ООП
  6. Результаты и выводы
  7. Спасибо за внимание

---

## ⚡ Быстрые улучшения (можно сделать за 1-2 часа каждое)

### 1. Двойной клик для редактирования
```cpp
// В MainWindow::setupTable() добавить:
connect(m_tableView, &QTableView::doubleClicked, 
        this, &MainWindow::onEditMachine);
```

### 2. Контекстное меню (ПКМ на строку)
```cpp
m_tableView->setContextMenuPolicy(Qt::CustomContextMenu);
connect(m_tableView, &QTableView::customContextMenuRequested,
        this, &MainWindow::showContextMenu);

void MainWindow::showContextMenu(const QPoint& pos) {
    QMenu menu;
    menu.addAction("Редактировать", this, &MainWindow::onEditMachine);
    menu.addAction("Удалить", this, &MainWindow::onDeleteMachine);
    menu.addSeparator();
    menu.addAction("Назначить на проект", this, &MainWindow::onAssignToProject);
    menu.exec(m_tableView->viewport()->mapToGlobal(pos));
}
```

### 3. Сортировка по колонкам
```cpp
// В MainWindow::setupTable() добавить:
m_tableView->setSortingEnabled(true);
```

### 4. Улучшение статусбара
```cpp
// Показывать количество отфильтрованных записей:
statusText = QString("Показано: %1 из %2 | Всего техники: %3")
            .arg(m_tableModel->rowCount())
            .arg(stats.total)
            .arg(stats.total);
```

---

## 🐛 Известные проблемы (TODO: исправить)

1. **Статусбар показывает неточные данные**
   - В `updateStatusBar()` используются хардкод-значения `1` и `24`
   - Исправить: взять из `m_tableModel->rowCount()`

2. **Нет проверки уникальности серийного номера**
   - При добавлении техники нужна валидация
   - Добавить метод `FleetDatabase::isSerialNumberExists(QString)`

3. **Нельзя отменить операции**
   - Нет Undo/Redo
   - Решение: использовать [`QUndoStack`](https://doc.qt.io/qt-6/qundostack.html) (сложно, можно пропустить)

---

## 📊 Метрики проекта

### Текущее состояние:
- **Строк кода:** ~2000 (без комментариев)
- **Файлов:** 14 (.h, .cpp, .ui)
- **Классов:** 6 (Machine, Project, FleetDatabase, MainWindow, MachineTableModel, и другие)
- **Функционал готов на:** ~40%

### Цель к завершению:
- **Строк кода:** ~4000
- **Файлов:** ~25
- **Классов:** ~12
- **Функционал готов на:** 100% (базовый) + 50% (расширенный)

---

## 🎯 План на следующие 3 дня

### День 1 (сегодня): Диалоги и поиск
- [x] README и ARCHITECTURE
- [ ] MachineDialog (добавление/редактирование)
- [ ] Поиск по технике
- [ ] Контекстное меню и двойной клик

### День 2: Проекты и назначение
- [ ] ProjectDialog
- [ ] ProjectsView  
- [ ] AssignDialog
- [ ] Улучшить статусбар

### День 3: История и документация
- [ ] HistoryView
- [ ] Диаграмма классов UML
- [ ] Начать пояснительную записку
- [ ] Финальное тестирование

---

**Статус:** Roadmap составлен, начинаем работу над Milestone 2  
**Приоритет 1:** Диалог добавления техники
